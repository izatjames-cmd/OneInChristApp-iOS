const {
  OLD_TESTAMENT_BIBLE_READING_PASSAGES
} = require(
  './oldTestamentPassages'
)

const {
  NEW_TESTAMENT_BIBLE_READING_PASSAGES
} = require(
  './newTestamentPassages'
)

module.exports = {
  OLD_TESTAMENT_BIBLE_READING_PASSAGES,
  NEW_TESTAMENT_BIBLE_READING_PASSAGES
}
